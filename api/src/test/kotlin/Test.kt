import org.apache.commons.lang3.time.DateUtils
import java.util.concurrent.TimeUnit
import kotlin.test.Test

class Test {
    @Test
    fun t() {

//        val totalSecsElapsed = 1201
//        val hours2 = totalSecsElapsed / 3600
//        val minutes2 = (totalSecsElapsed % 3600) / 60
//        val seconds2 = totalSecsElapsed % 60
//        val timeString2 = String.format("%02d:%02d:%02d", hours2, minutes2, seconds2)

//        var totalSecs = 86501
//        var hours = totalSecs / 3600;
//        var minutes = (totalSecs % 3600) / 60;
//        var seconds = totalSecs % 60;
//
//        var timeString = String.format("%02d:%02d:%02d", hours, minutes, seconds);
//
//        println(TimeUnit.SECONDS.toHours(86501))
//        println(TimeUnit.SECONDS.toMinutes(86501))
//        println(TimeUnit.SECONDS.toSeconds(86501))
//        println(timeString)
    }
}